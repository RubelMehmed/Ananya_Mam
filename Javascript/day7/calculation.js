// without user intput calculation
// without parameter

function calculation() {
  let a = 5;
  let b = 6;
  let c = 9;
  const add = a + b + c;
  const sub = a - b - c;
  const mul = a * b * c;
  const div = a / b / c;
  document.write(
    "addditon: " +
      add +
      "</br>" +
      " subtraction: " +
      sub +
      "</br>" +
      "multiplication: " +
      mul +
      "</br>" +
      "Divide: " +
      div
  );
}
calculation();
